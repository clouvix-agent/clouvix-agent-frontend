/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'upload.wikimedia.org',
      'github.githubassets.com',
      'www.vectorlogo.zone'
    ],
  },
}

module.exports = nextConfig 